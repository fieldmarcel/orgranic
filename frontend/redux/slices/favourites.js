import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    favourites: ,
};


export const favouritesSlice = createSlice({
    name: "favourites",
    initialState: {
        favourites: [],
   } })